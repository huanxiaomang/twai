{
  "name": "twai",
  "version": "1.0.0",
  "private": true,
  "description": "Tweet AI RSS Reader",
  "scripts": {
    "dev": "pnpm run --filter @twai/web dev",
    "build": "pnpm run --filter @twai/web build && pnpm run --filter @twai/api build",
    "preview": "pnpm run --filter @twai/web preview"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "workspaces": [
    "packages/*",
    "web"
  ]
}
